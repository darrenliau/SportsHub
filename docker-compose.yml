version: '3.8'
services:
  api:
    build: ./backend
    ports:
      - "5000:80"
    volumes:
      - ./backend:/app
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app

# Note: Replace build sections with proper Dockerfiles after scaffolding projects.
